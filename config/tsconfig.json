{
  // include 用来制定哪些ts文件需要被编译
  // 路径: ** 表示任意目录                    
  //       * 表示任意文件
  "include": [
    "./**/*" // 编译config下所有目录下所有文件
  ],

  // 不编译的目录文件
  "exclude": [
    "./static.ts" // 不编译config下的static.ts
  ]
}